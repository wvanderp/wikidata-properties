{
  "name": "wikidata-properties",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "wtf_wikipedia": "^8.4.0"
  },
  "devDependencies": {
    "parcel": "^1.12.4",
    "typescript": "^4.0.2",
    "ts-node": "^9.0.0",
    "@types/node": "^14.6.4",
    "rimraf": "^3.0.2"
  },
  "scripts": {
    "scrape": "ts-node scrape.ts",
    "build:parcel": "parcel build index.ts",
    "build:tsc": "tsc --outDir dist -d ",
    "build": "npm run build:tsc && npm run build:parcel && rimraf dist/data"
  }
}
